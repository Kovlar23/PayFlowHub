# Tests

This folder will host:

- domain unit tests for payment invariants and routing rules;
- integration tests for persistence and messaging boundaries;
- contract tests for the internal provider execution surface;
- end-to-end flow tests once the first payment API is implemented.

The first scaffold step focuses on architecture and runnable service baselines, so test projects will be introduced alongside the first concrete domain and API behavior.
